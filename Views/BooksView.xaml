<UserControl x:Class="Bibliothek.Views.BooksView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <DockPanel Margin="0,0,0,16">
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                <Button Content="Neu" Command="{Binding AddCommand}" />
                <Button Content="Bearbeiten" Command="{Binding EditCommand}" Style="{StaticResource SecondaryButton}" />
                <Button Content="Details" Command="{Binding DetailsCommand}" Style="{StaticResource SecondaryButton}" />
                <Button Content="Loeschen" Command="{Binding DeleteCommand}" Style="{StaticResource DangerButton}" />
            </StackPanel>
            <StackPanel>
                <TextBlock Text="Buecher" FontSize="24" FontWeight="SemiBold" />
                <TextBlock Text="Suche, Sortierung, Filter und CRUD" Foreground="{StaticResource BrushMuted}" />
            </StackPanel>
        </DockPanel>

        <TextBox Grid.Row="1" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,14" />

        <DataGrid Grid.Row="2" ItemsSource="{Binding ItemsView}" SelectedItem="{Binding SelectedItem}"
                  Background="#2D2D2D"
                  Foreground="#F0F0F0"
                  RowBackground="#2D2D2D"
                  AlternatingRowBackground="#333333"
                  BorderBrush="#444444"
                  GridLinesVisibility="Horizontal"
                  HorizontalGridLinesBrush="#444444"
                  VerticalGridLinesBrush="#444444">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Titel" Binding="{Binding Titel}" Width="*" />
                <DataGridTextColumn Header="Autor" Binding="{Binding Autor.Name}" Width="180" />
                <DataGridTextColumn Header="Verlag" Binding="{Binding Verlag.Name}" Width="180" />
                <DataGridTextColumn Header="Erscheinungsjahr" Binding="{Binding Erscheinungsjahr}" Width="130" />
                <DataGridTextColumn Header="ISBN" Binding="{Binding ISBN}" Width="170" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
